/* The snag screens' long-standing import path. The implementation lives in
 * lib/useBlobUrl so the evidence viewer shares exactly one copy of it. */
export { useBlobUrl, useBlobSource, type BlobState } from '../lib/useBlobUrl';
