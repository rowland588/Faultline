/// <reference types="vite/client" />

/** Build timestamp injected by vite.config.ts — shown on Home so it's possible
 *  to tell at a glance whether a device is running the latest deploy. */
declare const __BUILD_STAMP__: string;
